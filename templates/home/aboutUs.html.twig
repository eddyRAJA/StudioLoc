{% extends 'base.html.twig' %}

{% block title %}AboutUs{% endblock %}

{% block body %}
  {#<style>
    .example-wrapper {
      margin: 1em auto;
      max-width: 800px;
      width: 95%;
      font: 18px/1.5 sans-serif;
    }
    .example-wrapper code {
      background: #F5F5F5;
      padding: 2px 6px;
    }
  </style>#}

  <div class="container">
    <h1>About us</h1>
    {% for aboutUs in allOfUs %}

      <div class="container">
        <h3 class="mt-0">
          {{ aboutUs.title }}</h3>
        <div class="text-center"><img class="img-fluid" height="600" src="{{ aboutUs.poster }}" alt="poster of {{ aboutUs.title }} "></div>

        <p>Description :
          {{ aboutUs.description }}</p>
        {# 
      <p>Created on:
        {{ company.createdAt }}</p>
      <p>Updated on:
        {{ company.updatedAt }}</p>
     #}
        <p>slug :
          {{ aboutUs.slug }}</p>
      </div>
    {% endfor %}

  </div>
{% endblock %}