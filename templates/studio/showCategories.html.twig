{% extends 'base.html.twig' %}

{% block title %}AllCategories{% endblock %}

{% block body %}
  {#<style>
    .example-wrapper {
      margin: 1em auto;
      max-width: 800px;
      width: 95%;
      font: 18px/1.5 sans-serif;
    }
    .example-wrapper code {
      background: #F5F5F5;
      padding: 2px 6px;
    }
  </style>#}

  <div class="container">
    <h1>All categories of studios</h1>

    {% for category in categories %}
      <div class="container">
        <h3 class="mt-0">
          {{ category.name }}
          studios</h3>
        <div class="text-center"><img class="align-self-start mr-3" height="400" src="{{ category.poster }}" alt="poster of {{ category.name }} "></div>

        <p>Description :
          {{ category.description }}</p>
        {# 
      <p>Created on:
        {{ company.createdAt }}</p>
      <p>Updated on:
        {{ company.updatedAt }}</p>
    
        <p>slug :
          {{ category.slug }}</p> #}
      </div>
      <div>
        <p>
          <a href="{{ path('category_show', {'id': category.id}) }}" class="btn btn-lg btn-primary">More</a>
        </p>
      </div>

    {% endfor %}

  </div>
{% endblock %}