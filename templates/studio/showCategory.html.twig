{% extends 'base.html.twig' %}

{% block title %}oneCategory{% endblock %}

{% block body %}
  <h1>{{ category.name }}
    studios</h1>

  {% for studio in studios %}
    <div class="container">
      <h3 class="mt-0">
        {{ studio.name }}
        studio</h3>
      <div class="text-center">
        <img class="align-self-start mr-3" height="400" src="{{ studio.poster }}" alt="poster of {{ studio.name }} ">
      </div>
      <p>Summary :
        {{ studio.summary|raw }}</p>
      {# 
      <p>detail :
        {{ studio.detail }}</p>
       <p>unitPrice :
        {{ studio.unitPrice }}</p>
     
      <p>Created on:
        {{ company.createdAt }}</p>
      <p>Updated on:
        {{ company.updatedAt }}</p>
    
        <p>slug :
          {{ category.slug }}</p> #}
      <div>
        <p>
          <a href="{{ path('studio_show', {'id': studio.id}) }}" class="btn btn-lg btn-primary">Detail</a>
        </p>
      </div>
    </div>
  {% endfor %}

  {#</div>
<div class="container">
  <pre>
    {{ dump(studios) }}
</pre>
</div>#}

{% endblock %}