{% extends 'base.html.twig' %}

{% block title %}New Reservation{% endblock %}

{% block body %}
  <h1>Create new Reservation</h1>
  {{ reservation.author }}, you have choose to rent
  {{ studio.name }}
  ({{ reservation.id }}) studio:

  {{ include('reservation/_form.html.twig') }}
</br>
<a href="{{ path('user_show', {'id': reservation.author.id}) }}" class="btn btn-outline-primary">My preferences</a>

{#<a href="{{ path('reservation_index') }}">back to list</a>#}

{% endblock %}
